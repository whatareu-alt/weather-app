package com.example.aiweathermonitor.theme

// Color tokens intentionally empty — the app uses the plain Material 3 color
// scheme (see Theme.kt). Add semantic colors here as the new design takes shape.
